```mermaid
flowchart LR
    교수[/교수/]
    
    subgraph 성적처리 시스템
        성적입력([성적입력])
        학점조회([학점조회])
        교수체크([교수체크])
    end

    교수 --- 성적입력
    교수 --- 학점조회
    성적입력 -. "<< include >>" .-> 교수체크
    학점조회 -. "<< include >>" .-> 교수체크